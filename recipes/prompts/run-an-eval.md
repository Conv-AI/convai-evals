# Run An Eval

You are operating `@convai/evals`. Validate the scenario first with `convai-evals validate <scenario>`. If it is invalid, patch only the reported JSON paths. Then start the local worker build, server, and web UI. Run the scenario against the specified safe character and export the JSON report. Summarize pass rate, failed rows, backend IDs, and report path.
